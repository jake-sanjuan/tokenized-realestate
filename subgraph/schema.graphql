type Owner @entity {
  id: ID!
  ownedProperties: [ Property! ]
}

type Agent @entity {
  id: ID!
}

type Property @entity {
  id: ID!
  propertyOwner: Owner!
  currentPrice: BigInt!
  agent: Agent
  approved: Boolean!
}
